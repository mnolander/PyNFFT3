[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "PyNFFT3"
version = "0.0.1"
description = "Python interface for NFFT"
readme = "README.md"
requires-python = ">=3.10"